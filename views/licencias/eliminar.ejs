<div class="contenidor">
    <div class="contingut">
        <br>
        <div>
            <form method="POST" action="/licencias/eliminar">
                <input hidden type="text" name="licencia" value="<%= licencia %>">
                <h4 class="gestiounidad">¿Seguro que desea eliminar la licencia<br><br>
                    <div class="removeupdateunitat"><%= licencia %></div><br>
                    de la lista de Unidades?
                </h4>
                <br>
                <div class="btn btn-group rounded">
                    <button id="cancelar" type="button" class="btn btn-secondary" onclick="cancelar()">Cancelar</button>
                </div>
                <div class="btn btn-group rounded">
                    <button id="eliminar" type="submit" class="btn btn-warning">Eliminar</button>
                </div>

            </form>
        </div>
    </div>
</div>

<script>
    window.addEventListener('load', () => {

        $('#cancelar').click(function () {
            window.location.href = '/licencias'
        })

    })
</script>