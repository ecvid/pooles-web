<div class="contenidor">
    <div class="contingut">
        <br>
        <div>
            <form method="POST" action="/sustitutos/eliminar">
                <input hidden type="text" name="sustituto" value="<%= sustituto %>">
                <h4 class="gestiounidad">¿Seguro que desea eliminar el sustituto<br><br>
                    <div class="removeupdateunitat"><%= sustituto %></div><br>
                    de la lista de Sustitutos?
                </h4>
                <br>
                <div class="btn btn-group rounded">
                    <button id="cancelar" type="button" class="btn btn-secondary" onclick="cancelar()">Cancelar</button>
                </div>
                <div class="btn btn-group rounded">
                    <button type="submit" class="btn btn-warning">Eliminar</button>
                </div>
            </form>
        </div>
    </div>
</div>

<script>
    window.addEventListener('load', () => {

        $('#cancelar').click(function () {
            window.location.href = '/sustitutos'
        })

    })
</script>